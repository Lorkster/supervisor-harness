Run this task under the supervisor harness: $ARGUMENTS

Follow the `supervisor` rule: start the run with the task verbatim, dispatch the
returned packets in parallel with their briefs unmodified, report each result as
produced, advance between phases, let the user approve the proposed tasks, and
finish by presenting the verified definition-of-done results.
