Run this task under the supervisor harness: $ARGUMENTS

Follow the `supervisor` rule: check `supervisor_runs` for a run already in
flight and resume it rather than starting a second, dispatch the returned
packets in parallel with each agent reading `brief_path` and writing its answer
to `result_path`, report by `result_path`, advance between phases, let the user
approve the proposed tasks, and finish by presenting the verified
definition-of-done results.
